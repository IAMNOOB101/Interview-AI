// OTP Service - generates and verifies OTP tokens
export const sendOTP = async (email) => {
  // Stub implementation
  return { success: true, message: "OTP sent" };
};

export const verifyOTP = async (email, otp) => {
  // Stub implementation
  return { success: true, verified: true };
};

